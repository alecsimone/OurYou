import styled from 'styled-components';

const StyledFinishReset = styled.section`
  margin: 3rem auto;
  border-radius: 0.5rem;
  background: ${(props) => props.theme.midBlack};
  padding: 3rem;
  width: 100rem;
  max-width: 90%;
  h3 {
    margin: 0;
    text-align: center;
  }
`;

export default StyledFinishReset;
